sleep：不释放同步锁。可以让比自己优先级低的线程获得运行的机会
yeild：与sleep类似，但是不能指定暂停多久时间，只能让优先级相同或者高的获得执行机会。与sleep相似不会释放对象锁。
关于相同优先级调度的问题：不一定是分时的，因为也可能出现一个线程完全执行完毕另一个才执行，这样就是两个顺序执行了。


-------------------------------------
补充wait与notify：
它们是用于协调多个线程共享数据的存取的，所以必须使用在synchronized内部。wait会释放对象锁。